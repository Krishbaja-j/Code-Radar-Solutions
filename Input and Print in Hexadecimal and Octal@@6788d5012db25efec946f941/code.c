#include <stdio.h>

int main() {
    int x;
    scanf("%d", &x);

    printf("Hexadecimal: %X\n", x);  // Prints the number in uppercase hexadecimal
    printf("Octal: %o\n", x);        // Prints the number in octal

    return 0;
}
